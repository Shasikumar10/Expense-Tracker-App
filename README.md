# Expense Tracker App - Backend

This repository contains the backend scaffold for the Expense Tracker mobile app (Express + MongoDB).

Current status: initial backend scaffold (server, models, config). Next: add routes, auth, email, OAuth, and connect frontend.

See `.env.example` for environment variables.
